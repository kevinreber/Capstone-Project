// Use Tagify for keywords UI
// const keywords = new Tagify(document.querySelector("input[name=hero"));

const keywords = document.querySelector("input[name=keywords");

const keywordsTagify = new Tagify(keywords);